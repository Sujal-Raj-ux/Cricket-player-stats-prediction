# Build-time variable for the browser (never put DB secrets here).
# Local: http://127.0.0.1:8000
# Production: https://your-api-host.example.com  (no trailing slash)
VITE_API_URL=http://127.0.0.1:8000
